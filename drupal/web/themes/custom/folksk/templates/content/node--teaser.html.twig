<article{{ attributes.addClass('node--view-mode-teaser') }}>
  <div class="node__content">

    <div class="node-type-label">{{ node.type.entity.label }}</div>

    <h2 class="node__title">
      <a href="{{ url }}">{{ label }}</a>
    </h2>

    <div class="node__meta">
      {{ node.createdtime|date('d. m. Y') }}
      {% if author_name %} — {{ author_name }}{% endif %}
    </div>

    {% if content.body %}
      <div class="field--name-body">{{ content.body }}</div>
    {% endif %}

    <div class="node__readmore">
      <a href="{{ url }}">Čítať ďalej →</a>
    </div>

  </div>
</article>
