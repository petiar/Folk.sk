<div class="folk-submit-block">
  <p class="folk-submit-block__description">{{ description }}</p>
  <div class="d-flex flex-column gap-2">
    <a href="{{ url }}" class="btn btn-primary">Pridať článok</a>
    <a href="{{ event_url }}" class="btn btn-outline-primary">Pridať akciu do kalendára</a>
  </div>
</div>
